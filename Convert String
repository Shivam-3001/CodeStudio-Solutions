#include <bits/stdc++.h> 
string convertString(string str) 
{
	// WRITE YOUR CODE HERE
	str[0]=toupper(str[0]);
	for(int i=1; i<str.size(); i++){
		if(str[i]==' '){
			str[i+1]=toupper(str[i+1]);
			i++;
		}
	}

	return str;
}
