int stringCount(int n, string &s) {
	// Write your code here.
	int cnt=0;
	for(int i=0; i<s.size()-1; i++){
		string temp=s.substr(i, 2);
		if(temp=="*#" || temp=="#*"){
			cnt++;
		}
	}

	return cnt;
}
